{
  "name": "invoice-backend",
  "version": "1.0.0",
  "description": "AI extraction backend for the invoice and construction BOQ tools.",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "check": "node --check server.js",
    "test:app": "node rental-app/test/e2e.mjs",
    "audit:app": "node rental-app/audit.mjs",
    "test:db": "supabase/tests/run.sh",
    "build:app": "cp rental-app/ptas.html rental-app/index.html"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "pg": "^8.13.0",
    "playwright": "^1.49.0"
  }
}
