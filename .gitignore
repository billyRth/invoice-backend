# dependencies
node_modules/

# environment / secrets — never commit these
.env
.env.*
!.env.example

# logs
npm-debug.log*
*.log

# OS / editor noise
.DS_Store
.vscode/
.idea/

# Generated by "npm run build:app" for hosts that need a literal index.html.
# Never committed: the copy that used to live here fell a dozen commits behind.
rental-app/index.html
